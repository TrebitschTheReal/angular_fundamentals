import { AuthorNameValidatorDirective } from './author-name-validator.directive';

describe('AuthorNameValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new AuthorNameValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
